// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/osasadev-lab/aibo_pj/server/ent/task"
	"github.com/osasadev-lab/aibo_pj/server/ent/taskmention"
	"github.com/osasadev-lab/aibo_pj/server/ent/user"
)

// TaskMention is the model entity for the TaskMention schema.
type TaskMention struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// TaskID holds the value of the "task_id" field.
	TaskID uuid.UUID `json:"task_id,omitempty"`
	// MentionedUserID holds the value of the "mentioned_user_id" field.
	MentionedUserID uuid.UUID `json:"mentioned_user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaskMentionQuery when eager-loading is set.
	Edges        TaskMentionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaskMentionEdges holds the relations/edges for other nodes in the graph.
type TaskMentionEdges struct {
	// Task holds the value of the task edge.
	Task *Task `json:"task,omitempty"`
	// MentionedUser holds the value of the mentioned_user edge.
	MentionedUser *User `json:"mentioned_user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaskMentionEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// MentionedUserOrErr returns the MentionedUser value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaskMentionEdges) MentionedUserOrErr() (*User, error) {
	if e.MentionedUser != nil {
		return e.MentionedUser, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "mentioned_user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TaskMention) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taskmention.FieldCreatedAt, taskmention.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case taskmention.FieldID, taskmention.FieldTaskID, taskmention.FieldMentionedUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TaskMention fields.
func (_m *TaskMention) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taskmention.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case taskmention.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case taskmention.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case taskmention.FieldTaskID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field task_id", values[i])
			} else if value != nil {
				_m.TaskID = *value
			}
		case taskmention.FieldMentionedUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field mentioned_user_id", values[i])
			} else if value != nil {
				_m.MentionedUserID = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TaskMention.
// This includes values selected through modifiers, order, etc.
func (_m *TaskMention) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTask queries the "task" edge of the TaskMention entity.
func (_m *TaskMention) QueryTask() *TaskQuery {
	return NewTaskMentionClient(_m.config).QueryTask(_m)
}

// QueryMentionedUser queries the "mentioned_user" edge of the TaskMention entity.
func (_m *TaskMention) QueryMentionedUser() *UserQuery {
	return NewTaskMentionClient(_m.config).QueryMentionedUser(_m)
}

// Update returns a builder for updating this TaskMention.
// Note that you need to call TaskMention.Unwrap() before calling this method if this TaskMention
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TaskMention) Update() *TaskMentionUpdateOne {
	return NewTaskMentionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TaskMention entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TaskMention) Unwrap() *TaskMention {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TaskMention is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TaskMention) String() string {
	var builder strings.Builder
	builder.WriteString("TaskMention(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("task_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TaskID))
	builder.WriteString(", ")
	builder.WriteString("mentioned_user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.MentionedUserID))
	builder.WriteByte(')')
	return builder.String()
}

// TaskMentions is a parsable slice of TaskMention.
type TaskMentions []*TaskMention
